{% extends 'base.html.twig' %}

{% block title %}Editar Banco{% endblock %}

{% block body %}

    <div class="container">
        <div class="row h-50 justify-content-center align-items-center">
            <div class="col-4 rounded my-1 p-2 bg-light text-dark">
                <h1>Editar Banco</h1>
                {{ include('banco/_form.html.twig', {'button_label': 'Editar'}) }}
                <div class="row">
                    <div class="col-md-2">
                        <a class="btn btn-primary" href="{{ path('app_banco_index') }}">Atras</a>
                    </div>
                    <div class="col">
                        {{ include('banco/_delete_form.html.twig') }}
                    </div>
                </div>
            </div>
        </div>
    </div>

{% endblock %}
