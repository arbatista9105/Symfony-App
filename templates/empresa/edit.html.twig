{% extends 'base.html.twig' %}

{% block title %}Editar Empresa{% endblock %}

{% block body %}

    <div class="container">
        <div class="row h-50 justify-content-center align-items-center">
            <div class="col-4 rounded my-1 p-2 bg-light text-dark">
                <h1>Editar Empresa</h1>
                {{ include('empresa/_form.html.twig', {'button_label': 'Editar'}) }}
                <div class="row">
                    <div class="col-md-2">
                        <a class="btn btn-primary" href="{{ path('app_empresa_index') }}">Atras</a>
                    </div>
                    <div class="col">
                        {{ include('empresa/_delete_form.html.twig') }}
                    </div>
                </div>
            </div>
        </div>
    </div>

{% endblock %}
