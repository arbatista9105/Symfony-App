{% extends 'base.html.twig' %}

{% block title %}Empresa{% endblock %}

{% block body %}
    <h2>Empresa</h2>

    <table class="table table-striped">
        <tbody>
            <tr>
                <th>Name</th>
                <td>{{ empresa.name }}</td>
            </tr>
        </tbody>
    </table>
    <div class="container m-0">
        <div class="row">
            <div class="col-md-1">
                <a class="btn btn-primary" href="{{ path('app_empresa_index') }}">Atras</a>
            </div>
            <div class="col-md-1">
                <a class="btn btn-primary" href="{{ path('app_empresa_edit', {'id': empresa.id}) }}">Editar</a>
            </div>
            <div class="col-md-1">
                {{ include('empresa/_delete_form.html.twig') }}
            </div>
        </div>
    </div>

{% endblock %}
